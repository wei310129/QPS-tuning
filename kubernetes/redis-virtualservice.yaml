apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: mps-redis-virtualservice
  namespace: front-mpos
spec:
  hosts:
    - "mps-redis-i-ut00.fbl.com.tw"
  gateways:
    - mps-redis-gateway             # 綁定到 Gateway，允許外部流量進入
  tcp:
    - match:
        - port: 26379
      route:
        - destination:
            host: mps-redis-sentinel-service
            port:
              number: 26379
